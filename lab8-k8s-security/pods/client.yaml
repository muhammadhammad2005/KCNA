apiVersion: v1
kind: Pod
metadata:
  name: allowed-client
  namespace: security-lab
  labels:
    access: allowed
spec:
  containers:
  - name: box
    image: busybox
    command: ["sleep","3600"]
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"

